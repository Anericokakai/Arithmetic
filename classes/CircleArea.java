public class circle {
    public class Circle {
        private double radius;
    
        public Circle(double radius) {
            this.radius = radius;
        }
    
        public double area() {
            return 3.14159 * Math.pow(this.radius, 2);
        }
    }
    
        
    }
// modified the area calculation using math.pow    